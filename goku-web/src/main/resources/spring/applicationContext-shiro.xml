<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd">

    <!-- Subject工厂 -->
    <bean id="subjectFactory" class="com.yongle.goku.system.shiro.StatelessDefaultSubjectFactory"/>

    <!-- 会话管理器 -->
    <bean id="sessionManager" class="org.apache.shiro.session.mgt.DefaultSessionManager">
        <property name="sessionValidationSchedulerEnabled" value="false"/>
    </bean>

    <!-- 安全管理器 -->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <property name="realm" ref="tokenRealm"/>
        <property name="subjectDAO.sessionStorageEvaluator.sessionStorageEnabled" value="false"/>
        <property name="subjectFactory" ref="subjectFactory"/>
        <property name="sessionManager" ref="sessionManager"/>
    </bean>

    <!-- 相当于调用SecurityUtils.setSecurityManager(securityManager) -->
    <bean class="org.springframework.beans.factory.config.MethodInvokingFactoryBean">
        <property name="staticMethod" value="org.apache.shiro.SecurityUtils.setSecurityManager"/>
        <property name="arguments" ref="securityManager"/>
    </bean>

    <bean id="tokenAuthFilter" class="com.yongle.goku.system.shiro.filter.TokenAuthFilter">
        <property name="excludeUrl">
            <list>
                <value>/users/login</value>
            </list>
        </property>
    </bean>

    <!--&lt;!&ndash;Shiro的Web过滤器&ndash;&gt;-->
    <!--<bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">-->
    <!--<property name="securityManager" ref="securityManager"/>-->
    <!--<property name="filters">-->
    <!--<util:map>-->
    <!--<entry key="tokenAuthFilter" value-ref="tokenAuthFilter"/>-->
    <!--</util:map>-->
    <!--</property>-->
    <!--<property name="filterChainDefinitions">-->
    <!--<value>-->
    <!--/**=tokenAuthFilter-->
    <!--</value>-->
    <!--</property>-->
    <!--</bean>-->

    <bean id="filterChainManager" class="com.yongle.goku.system.shiro.filter.CustomDefaultFilterChainManager">
        <property name="customFilters">
            <util:map>
                <entry key="tokenAuthFilter" value-ref="tokenAuthFilter"/>
            </util:map>
        </property>
        <property name="defaultFilterChainDefinitions">
            <value>
                /** = tokenAuthFilter,sysUser
            </value>
        </property>
    </bean>

    <!-- Shiro生命周期处理器-->
    <bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"/>
</beans>